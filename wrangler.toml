# wrangler.toml - Cloudflare Pages Configuration

name = "elc-website-blv"
pages_build_output_dir = ".next/static"

# Environment variables
[env.production.vars]
ENVIRONMENT = "production"
NODE_ENV = "production"
